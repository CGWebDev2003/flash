# FLASH CLI

Flash is your lightweight CLI-Tool for projects. Create folders, files and prjects over Commandline.

## Install

### Install

```bash
npm install -g .
```

or use

```bash
npm install -g flash
```
